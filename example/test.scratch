sprite Stage {}

sprite cat {
    require "time.scratch";
    require "console.scratch";

    func main(){
        consoleClear();
        while (true){
            console.remove(0);
            print(DT);
        }
    }
}