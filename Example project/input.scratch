create Stage{
    script[
        var thing = 2 + 4;
    ];
    costumes[
        "blank.svg"
    ];
    sounds[];
}

create cat{
    script[
        event_whenflagclicked();

        motion_movesteps(5 + thing);

        control_if(operator_equals(1, 3 + 1)){
            motion_gotoxy(10, 7);
        };

        list what = 10;
        
        thing = what;

        thing = 2;
        thing += thing * (2 / 3);
        thing = 25 + 3 + thing;
    ];
    costumes[
        "cat.svg"
    ];
}