sprite Stage{}

sprite cat{
    script[
        start();
        list distances;# = [10, 23, 56, 11];

        var i = 0;
        while (i < len(distances)) {
            if(i != 1){
                move(distances[i]);
            };
            i++;
        };
    ];
    costumes[
        "pics/cat.svg"
    ];
}