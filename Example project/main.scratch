sprite Stage{}

sprite cat{
    script[
        event_whenflagclicked();
        motion_gotoxy(0, 0);
        
        list distances = [10, 3, 54, 7];

        var i = 0;
        while(i < len(distances)){
            motion_movesteps(distances[i]);
            i++;
        };
    ];
    costumes[
        "pics/cat.svg"
    ];
}