sprite Stage {}

sprite cat {
    costume cat "cat.svg";

    require "std.scratch";
    require "math.scratch";
    
    func main(){
        ptr num << pow(10, 2);
        num = sqrt(num);
        print(num);
        print(len(num));
    }
}