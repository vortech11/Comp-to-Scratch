sprite Stage{}

sprite cat{
    script[
        func testfunc(input1: text){
            static thing;
            move(input1);
            return 10;
        }
        
        start();
        move(testfunc(10) + testfunc(4));
        move(3);
    ];
    costumes[
        "pics/cat.svg"
    ];
}