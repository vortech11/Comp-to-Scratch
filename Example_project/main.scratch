sprite Stage{};

sprite cat{
    script[
        start();
        setxy(0, 0);
        list distances = [10, 23, 56, 11];

        for(var i = 0; i < len(distances); i++){
            if(i != 1){
                move(distances[i]);
            };
            sleep(".5");
        };
    ];
    costumes[
        "pics/cat.svg"
    ];
};